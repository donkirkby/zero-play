language: python
python:
    - "3.8"
install:
    - "pip install pipenv"
    - "pipenv install --dev"
script:
    - mypy zero_play && QT_DEBUG_PLUGINS=1 coverage run --source=zero_play -m pytest -s
after_success:
    - pip install codecov
    - codecov